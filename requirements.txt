TASK: Make Buy Account + Buy Sessions UI Fully Configurable

Upgrade ONLY the storefront UI/configuration for:

- 🛒 Buy Account
- 🔐 Buy Sessions

Do not change existing payment, stock, purchase, authentication, OTP/2FA, or delivery logic.

1. FULL MESSAGE EDITING

Admin panel me separate settings add karo:

🛒 Buy Account Message

- Admin poora message/caption edit kar sake.
- HTML/Markdown formatting supported ho.
- Default message current polished Account Store message ho.

🔐 Buy Sessions Message

- Admin poora message/caption independently edit kar sake.
- Account message aur Sessions message ek dusre se independent hon.

Admin ko sirf predefined short fields edit karne tak restrict mat karo — poora caption/message editable hona chahiye.

2. BANNER IMAGE

Dono stores ke liye independent configurable banner:

- Account Store Banner
- Sessions Store Banner

Admin controls:

🖼 Set Banner
👁 Preview Banner
🗑 Remove Banner
↩️ Back

If no banner is configured, gracefully send the store message without an image.

Do not hard-code an external image URL.

3. ALL INLINE BUTTON TEXT MUST BE EDITABLE

Admin panel me ek dedicated:

⚙️ Store Buttons

section banao.

Separate editable labels for every button.

For Account Store:

- Product button template
- Buy Now
- Back
- Previous
- Next
- Cancel
- Page indicator if applicable

For Sessions Store:

- Product button template
- Quantity buttons
- Confirm
- Change Quantity
- Back
- Previous
- Next
- Cancel

Admin ko har button ka displayed text change karne dena.

Example:

Current:
"🇲🇽 Mexico"

Admin changes it to:
"🇲🇽 Mexico Accounts"

The callback/functionality must remain unchanged.

4. ALL PRODUCT BUTTONS ON ONE PAGE

Important UI change:

If the number of available products fits within Telegram's practical inline-keyboard limits, show all available products on the same page.

Use a compact 2-column layout where possible:

🇲🇽 Mexico      🇲🇦 Morocco
🇹🇭 Thailand    🇺🇸 USA Frozen
🇺🇸 USA Harsh

Do not unnecessarily create pagination when all products can fit on one page.

If there are too many products to safely fit, automatically fall back to pagination.

Pagination must continue using the existing safe callback mechanism.

5. BUTTON ORDER / LAYOUT

Make the layout configurable where practical.

Default:

[ Product ] [ Product ]
[ Product ] [ Product ]
[ Product ] [ Product ]

Then:

[ ⬅️ Back ]

For product details:

[ 🛒 Buy Now ]
[ ⬅️ Back to Products ]

Do not put unnecessary buttons on separate pages.

6. ADMIN PREVIEW

Add:

👁 Preview Account Store

and

👁 Preview Sessions Store

Preview should show the exact configured message, banner and button labels that users will see.

Preview must not execute a purchase.

7. PERSISTENCE

Store the configurable UI settings using the project's existing settings/database mechanism.

Do not introduce a second database system.

Suggested logical settings:

- account_store_message
- account_store_banner
- sessions_store_message
- sessions_store_banner
- account_button_labels
- sessions_button_labels

Use the project's existing settings architecture if equivalent keys already exist.

8. SAFETY / NON-REGRESSION

Do NOT modify:

- payment logic
- wallet/balance logic
- stock deduction logic
- existing purchase callbacks
- existing delivery logic
- OTP/2FA handling
- authentication/session handling
- admin permissions
- unrelated bot features

Reuse existing callbacks internally wherever possible.

Changing the visible button text must NOT break callback handling.

9. VALIDATION

Before finishing:

- Run "py_compile".
- Run "git diff --check".
- Search all existing Buy Account and Buy Sessions handlers/callbacks.
- Test 1, 2, 5 and many products.
- Test 2-column layout.
- Test pagination fallback.
- Test custom message.
- Test custom banner.
- Test every custom button label.
- Test Back/Buy/Next/Previous callbacks.
- Confirm existing purchase functionality remains unchanged.

At the end report:

1. Files changed
2. New admin settings
3. Editable messages
4. Editable banners
5. Editable button labels
6. One-page product layout
7. Pagination fallback
8. Validation performed
9. Confirmation that purchase/payment logic was not modifiedtelethon==1.41.2
aiohttp==3.12.15
